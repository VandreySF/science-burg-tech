{
  "name": "burger-tech",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build"
  }
}
