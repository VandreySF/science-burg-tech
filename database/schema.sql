-- Science Burger Tech - Database schema
-- Define your tables, relationships and constraints here.

-- Example:
-- CREATE TABLE users (
--     id SERIAL PRIMARY KEY,
--     name VARCHAR(120) NOT NULL,
--     email VARCHAR(120) UNIQUE NOT NULL,
--     created_at TIMESTAMP DEFAULT NOW()
-- );
